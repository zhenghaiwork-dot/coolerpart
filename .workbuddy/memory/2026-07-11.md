# 2026-07-11

## 全站部署兼容性 + 询盘功能审计

### 修复清单
1. **创建 `public/.htaccess`** — Apache/PHP 共享主机部署支持（clean URL 重写 + Gzip + 缓存 + 安全头 + 404）
2. **Formspree 表单增强** — 添加 `_next` 重定向字段（JS 动态设当前域名）+ `_subject` 邮件主题字段
3. **Favicon mask-icon 颜色** — `#8D46E7`(紫色模板值) → `#0d9488`(teal 品牌色)
4. **config.yaml** — OpenGraph `site_name` 从 `AstroWind` → `CoolerParts`
5. **robots.txt** — 添加 sitemap 引用
6. **删除模板遗留页** — homes/(4) + landing/(6) + pricing + services + products/[slug]/ + [...blog]/ = ~76 页

### 构建结果
- astro check: 0 errors / 0 warnings / 0 hints
- astro build: 262 pages, 12MB, 32s
- dist/ 包含: .htaccess, _headers, robots.txt, sitemap-index.xml

### 关键待办（部署前）
- 替换 Formspree `YOUR_FORM_ID` 为真实 form ID
- 更新 config.yaml `site` 为真实域名
- 更新 robots.txt sitemap URL

### 部署兼容性结论
- Astro SSG `output: 'static'` → 纯静态 HTML/CSS/JS，无后端依赖
- 表单: Formspree 外部 SaaS，无需 PHP
- 询盘篮: localStorage 纯前端
- 可直接上传 dist/ 到 PHP/HTML 主机
